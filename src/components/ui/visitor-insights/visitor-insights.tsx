import { FC } from "react";
import { DashboardTitle } from "../dashboard-title";

export const VisitorInsights: FC = () => {
    return (
        <div>
            <DashboardTitle title="Visitor Insights" />
        </div>
    );
}